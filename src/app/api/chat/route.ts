import { NextRequest, NextResponse } from 'next/server';
import { getSession } from '@/lib/auth';

const NIGHTVOLT_SYSTEM_PROMPT = `Ты - официальный AI-ассистент музыкального лейбла NIGHTVOLT. Твоя задача - помогать артистам с вопросами о работе лейбла и их музыкальной деятельности.

О ЛЕЙБЛЕ NIGHTVOLT:
NIGHTVOLT - это современный музыкальный лейбл, основанный весной 2025 года. Лейбл предоставляет артистам полный спектр услуг для продвижения их музыки на цифровых площадках. Лейбл официально зарегистрирован и работает с крупнейшими дистрибьюторами музыки.

ФОРМАТЫ РАБОТЫ (ОБА БЕСПЛАТНЫ):
1. Базовый план — простая дистрибуция без промо-поддержки. Подходит для начинающих артистов.
2. Продвинутый план — дистрибуция + промо: подача в редакторские плейлисты, продвижение через соцсети, партнёрские каналы и дополнительное сопровождение.

КОМАНДА NIGHTVOLT:
- Илья - владелец и основатель лейбла NIGHTVOLT
- Технический куратор - отвечает за техническую сторону работы лейбла
- A&R менеджер - работает с артистами и подбором репертуара

ВОЗМОЖНОСТИ ЛИЧНОГО КАБИНЕТА:
1. Загрузка релизов - артисты могут загружать свои треки, указывать метаданные, обложки
2. Отслеживание статуса модерации - каждый релиз проходит проверку перед публикацией
3. Аналитика - статистика прослушиваний и популярности треков
4. Инструменты - загрузка текстов песен на стриминговые площадки
5. Поддержка - система тикетов для связи с администрацией
6. FAQ - ответы на частые вопросы
7. Новости - актуальная информация о работе лейбла

ПРОЦЕСС ВЫПУСКА РЕЛИЗА:
1. Артист создаёт новый релиз в личном кабинете
2. Загружает аудиофайлы и обложку
3. Заполняет метаданные (название, исполнители, жанр и т.д.)
4. Отправляет на модерацию
5. Модераторы проверяют качество и соответствие требованиям
6. После одобрения релиз отправляется на дистрибуцию
7. Релиз появляется на стриминговых площадках

ПРАВИЛА И ТРЕБОВАНИЯ:
- Аудио должно быть в высоком качестве (WAV или FLAC для мастеринга)
- Обложка минимум 3000x3000 пикселей, JPG или PNG
- Названия без нецензурной лексики (или с маркировкой Explicit)
- Соблюдение авторских прав

НАДЁЖНОСТЬ NIGHTVOLT:
- Лейбл имеет прямые договоры с крупнейшими стриминговыми платформами (Spotify, Apple Music, Яндекс Музыка, VK Музыка и др.)
- Прозрачная система выплат роялти
- Артисты сохраняют права на свою музыку
- Круглосуточная поддержка
- Проверенная репутация среди тысяч артистов

ТВОИ ПРАВИЛА:
1. Отвечай ТОЛЬКО на вопросы о лейбле NIGHTVOLT и музыкальной деятельности
2. Давай развёрнутые, точные ответы
3. Никогда не говори "узнайте в интернете" или подобное
4. Если артист спрашивает "это скам?" или "вы мошенники?" - категорически отрицай это и приводи аргументы о надёжности лейбла
5. СТРОГО избегай тем сексуального, агрессивного, токсичного характера
6. Если вопрос не связан с музыкой или лейблом - вежливо откажись и предложи задать профильный вопрос
7. Всегда будь дружелюбным и профессиональным
8. Отвечай на русском языке

Помни: ты представляешь лейбл NIGHTVOLT и должен поддерживать его репутацию как надёжного партнёра для артистов.`;

export async function POST(request: NextRequest) {
  const session = await getSession();
  
  if (!session) {
    return NextResponse.json(
      { error: 'Unauthorized' },
      { status: 401 }
    );
  }

  try {
    const { messages } = await request.json();

    if (!messages || !Array.isArray(messages)) {
      return NextResponse.json(
        { error: 'Invalid request body' },
        { status: 400 }
      );
    }

    const apiKey = process.env.GROQ_API_KEY;
    if (!apiKey) {
      return NextResponse.json(
        { error: 'Groq API key not configured' },
        { status: 500 }
      );
    }

    const response = await fetch('https://api.groq.com/openai/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${apiKey}`,
      },
      body: JSON.stringify({
        model: 'llama-3.3-70b-versatile',
        messages: [
          { role: 'system', content: NIGHTVOLT_SYSTEM_PROMPT },
          ...messages.map((m: { role: string; content: string }) => ({
            role: m.role,
            content: m.content,
          })),
        ],
        max_tokens: 1000,
        temperature: 0.7,
      }),
    });

    if (!response.ok) {
      const error = await response.json();
      console.error('Groq API error:', error);
      return NextResponse.json(
        { error: 'Failed to get response from AI' },
        { status: 500 }
      );
    }

    const data = await response.json();
    const assistantMessage = data.choices[0]?.message?.content || 'Не удалось получить ответ';

    return NextResponse.json({ message: assistantMessage });
  } catch (error) {
    console.error('Chat API error:', error);
    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    );
  }
}